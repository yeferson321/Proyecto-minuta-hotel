@page
@model Frontend.Pages.UserDataModel
@{
}

<table class="table">
    @foreach (var dato in Model.DatoUser)
    {
        <tr>
            <td>@dato.Id</td>
            <td>@dato.Email</td>
            <td>@dato.Password</td>
        </tr>
    }
</table>